import { metadataTransforms } from './metadata-transforms';

describe('metadataTransforms', () => {
  it('should work', () => {
    expect(metadataTransforms()).toEqual('metadata-transforms');
  });
});
